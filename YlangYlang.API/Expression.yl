let
  makeSame a b = [a, b]

  f x =
    let
      genX {y} =
        let
          _ = makeSame x [ y ]
        in
        ()
      in
      (makeSame x "bla", makeSame x 1)
in
f








--( a, (id, id "hi" , id 34.5), (tuplify, tuplify 'd'), (getIfTrue, getIfTrue {x=True,y= ()} ), fst )

